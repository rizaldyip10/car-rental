export type UserInputType = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
};

export type UserType = {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
    password: string;
};

export type UserSessionType = {
    id: string;
    email: string;
    photo: string;
    name: string;
}

export type OwnerType = {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
}